# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VendPractice' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'SnapKit', '~> 4.2'
  pod 'Swinject','2.6.0'
  pod 'SwinjectStoryboard'
  pod 'PINRemoteImage'

  target 'VendPracticeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VendPracticeUITests' do
    # Pods for testing
  end

end
